server:
  port: 8081

spring:
  application:
    name: zone-service

  datasource:
    url: jdbc:mysql://localhost:3306/agms
    username: root
    password: 1234

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka